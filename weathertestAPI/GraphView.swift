//
//  GraphView.swift
//  weathertestAPI
//
//  Created by 植田圭祐 on 2019/04/04.
//  Copyright © 2019 Keisuke Ueda. All rights reserved.
//

import UIKit
import Charts

class GraphView {
    
    /*func graphMake() {
        let sales = [18.0, 19.5, 16.4, 10.0, 12.2, 13.5, 13.8, 13.1]
        let times = ["18","21","24","3","6","9","12","15"]
        
        setChart(values: sales, time: times)
    }

    
    func setChart(values: [Double], time: [String]) {
        //ChartDataEntry型の配列を宣言
        var entry = [ChartDataEntry]()
        
        for i in 0..<values.count {
            //xにデータの値、yに横軸の値のセットを配列に追加
            entry.append(ChartDataEntry(x: Double(i), y: values[i] ))
        }
        
        let dataSet = LineChartDataSet(values: entry, label: nil)
        dataSet.colors = [UIColor.blue]
        dataSet.circleRadius = 4
        dataSet.circleColors = [UIColor.purple]
        
        let xAxis = lineChart.xAxis
        xAxis.valueFormatter = IndexAxisValueFormatter(values: time)
        //Viewにグラフを描画
        lineChart.data = LineChartData(dataSet: dataSet)
        
        
    }*/
    /*
    // MARK: - Navigation

    // In a storyboard-based application, you will often want to do a little preparation before navigation
    override func prepare(for segue: UIStoryboardSegue, sender: Any?) {
        // Get the new view controller using segue.destination.
        // Pass the selected object to the new view controller.
    }
    */

}
